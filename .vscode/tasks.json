{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with GTK4",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-o",
                "filesystem",
                "fgui.c",
                "`pkg-config --cflags --libs gtk4`"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}